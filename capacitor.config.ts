import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.perfumeselisa.admin',
  appName: 'Gestión Perfumes Elisa',
  webDir: 'dist'
};

export default config;
